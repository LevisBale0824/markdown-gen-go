{
  "name": "markdown-gen-go",
  "version": "0.1.0",
  "description": "A cross-platform Markdown editor with AI assistant (Go + Wails)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.6.0",
    "vite": "^7.3.1"
  }
}
